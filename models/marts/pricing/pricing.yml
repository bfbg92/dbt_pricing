version: 2

models:
  - name: dim_sku_turnaround_type
    description: "TODO"
    columns:
      - name: date_price_updated
        description: "Date when spider ran and prices were fetched from"
        tests:
          - not_null
      - name: country_name
        description: "Country of the shop where prices were fetched from"
        tests:
          - not_null
      - name: product_name
        description: "Product Name of the collected prices"
        tests:
          - not_null
      - name: sku_no_turnaround
        description: "Unique product id without turnaround information of the collected prices"
        tests:
          - not_null
      - name: turnaround_type
        description: "TODO"
        tests:
          - not_null
      - name: sku
        description: "Unique product id of the collected prices"
        tests:
          - not_null

  - name: fct_pricing
    description: "todo"
    columns:
      - name: date_price_updated
        description: "Date when spider ran and the pricing values were fetched"
        tests:
          - not_null
      - name: country_name
        description: "Country of the shop where prices where fetched"
        tests:
          - not_null
      - name: product_name
        description: "Product Name of the collected prices"
        tests:
          - not_null
      - name: sku
        description: "Unique product id of the collected prices"
        tests:
          - not_null
      - name: sku_no_turnaround
        description: "Unique product id without turnaround information of the collected prices"
        tests:
          - not_null
      - name: quantity
        description: "Product Attribute: Quantity"
        tests:
          - not_null
      - name: turnaround
        description: "Product Attribute: TurnAround"
        tests:
          - not_null
      - name: turnaround_type
        description: "todo"
        tests:
          - not_null
      - name: material
        description: "Product Attribute: material"
        tests:
          - not_null
      - name: size
        description: "Product Attribute: size"
        tests:
          - not_null
      - name: cover
        description: "Product Attribute: cover"
      - name: finishing
        description: "Product Attribute: finishing"
      {{ pricing_companies_columns() }}
      - name: cost_price
        description: "SKU Cost Price for Helloprint"
      - name: supplier_price
        description: "SKU Supplier Price for Helloprint"
      - name: carrier_cost
        description: "SKU Carrier Cost for Helloprint"
